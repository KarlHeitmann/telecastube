<!DOCTYPE html>
<html>
  <head>
    <title>Telecastube</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'shared/navbar' %>
    <% if params[:action] == "show" %>
      <h1>esto es el SHOW</h1>
      <h1> JA JA JA JA. Este es un buen chiste</h1>
    <% end %>
    <div class="container">
      <h1>Título <%= params[:controller] %></h1>
      <% if params[:controller] == "users" %>
        <h2>USUARIOOOOSSSSSS</h2>
        <% if params[:action] == "show" %>
          <h3> ESTA ES LA ACCION SHOW</h3>
        <% end %>
      <% elsif params[:controller] == "videos" %>
        <h2>los videos del telecastube</h2>
      <% end %>
      <%= yield %>
    </div>
    <%= debug params if Rails.env.development? %>
  </body>
</html>
